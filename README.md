# RAG Support

A Retrieval-Augmented Generation customer support assistant, built as a single
cohesive application rather than a pile of notebooks: data ingestion,
cleaning, vector storage, retrieval, and generation all live behind one
pipeline and one API.

> Work in progress — this README is being filled in as the project is built.
> See `docs/` for the growing set of design and setup documents.
